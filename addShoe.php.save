<?php
	// Create connection
	$conn = mysqli_connect("localhost", "team7", "Team_7", "shoes");

	// Check connection
	if($link === false){
    	die("ERROR: Could not connect. " . mysqli_connect_error());
	}
	else
/*

	$type 	= $_POST["sexVal"];
	$brand 	= $_POST["brandVal"];
	$color 	= $_POST["colorVal"];
	$size 	= $_POST["sizeVal"];
	$store 	= $_POST["storeVal"];

	$sql = "insert into shoe(brand_name, store_name, size, color, price, type) values('Adidas', 'Walmart (Lakewood, OH)', '9', 'Red', '500', 'Male')";

	if (mysqli_query($conn, $sql)) {
    echo "New record created successfully";
} else {
    echo "Error: " . $sql . "<br>" . mysqli_error($conn);
}


	$conn->close();
$sql = "insert into shoe(brand_name, store_name, size, color, price, type) values('Adidas', 'Walmart (Lakewood, OH)', '9', 'Red', '500', 'Male')";

	if(mysqli_query($link, $sql)){
    	echo "Records inserted successfully.";
	} else{
    	echo "ERROR: Could not able to execute $sql. " . mysqli_error($link);
	}
	*/
 
// Close connection
mysqli_close($link);
?>
